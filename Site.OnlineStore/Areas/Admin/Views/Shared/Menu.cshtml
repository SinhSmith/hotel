@model List<Portal.Model.ViewModel.MenuView>

@foreach (var item in Model)
{
    <li class="treeview" id=@("naviLink" + item.Id)>
        <a href=@item.Url><i class='fa @item.Icon'></i><span>@item.Name</span></a>
    </li>
}